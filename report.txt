1 er probleme : A la decompression si le dernier entier n'est pas rempli on a des entiers 0 qui ressortent qui n'etaient pas dans le tableau de base
solution :  ? taille original ?
            - stocker le nombre d'entiers dans le tableau compresse
? stocker le nombre de bits utilises pour representer chaque entier                                 DONE
    -> ou ? le tableau compresse ?  
        - on peur le mettre au debut du tableau compresse, sur 6 bits car un entier peut faire au max 32 bits

========================================================================================================================
Methode 1 : On recupere le max de l'array, on calcule le nombre de bits necessaires pour representer ce max -> k
    On transforme chaque entier en binaire sur k bits
    Puis on concatene tous les bits dans une chaine
    On segmente cette chaine en morceaux de 32 bits
    On convertit chaque morceau en entier et on le stocke dans un tableau d'entiers
    On retourne ce tableau d'entiers
    
    Pour la decompression on commence par lire la taille des entiers dans le tableau compresse (6 premiers bits du premier entier)
    Ensuite on recupere les entiers un par un en lisant k bits a la fois et en convertissant en entier
    La liste est modifiee en place

    Pour get(i) on

    Reste un probleme : si le dernier entier n'est pas rempli on a des entiers 0 qui ressortent qui n'etaient pas dans le tableau de base
    Solution : stocker le nombre d'entiers dans le tableau compresse ? pas ideal, si le tableau est tres grand
                Interdire les 0 a la fin du tableau ? 
========================================================================================================================
Methode 2 : On recupere le max de l'array, on calcule le nombre de bits necessaires pour representer ce max -> k
    On choisit un k' < k
    On ecrit k' dans les 6 premiers bits du tableau compresse
    Puis k dans les 6 bits suivants
    Puis la longueur du tableau d'entiers dans les 32 bits suivants         <------------- 32 ??
    On parcourt le tableau d'entiers, pour chaque entier on regarde si il peut etre represente sur k' bits
        Si oui on le convertit en binaire sur k' bits, on ajoute un bit de controle a 0 au debut
        Si non on le remplace par son indice dans une liste de "grands entiers" (entiers qui ne peuvent pas etre representes sur k' bits)
            On ajoute un bit de controle a 1 au debut, puis on ajoutera l'entier en binaire sur k bits a la fin du tableau compresse
    On concatene tous les bits dans une chaine
    On segmente cette chaine en morceaux de 32 bits
    On convertit chaque morceau en entier et on le stocke dans un tableau d'entiers
        


    Trouver le meilleur compromis entre le nombre d'entiers a stocker et le nombre de bits par entier
    il faut que ne nb d'entiers moins compresse rentre dans le codage d'un entierc compresse
    on 

    Pour la decompression, plusieurs methodes possibles:
        - 1 - Pour faire le premier prototype, j'ai commencé par décompresser la liste comme si il n'y avait pas 
            de "grands entiers", mais a chaque "grand entier" rencontré, j'ai stocké son indice dans une liste.
            Ensuite, j'ai parcouru cette liste d'indices et pour chaque indice, j'ai lu les bits nécessaires pour récupérer le "grand entier"
            et je l'ai inséré à la place de l'indice dans la liste décompressée.
            Cette méthode est simple à implémenter, mais elle nécessite de parcourir la liste décompressée deux fois.
        
        - 2 - 
        